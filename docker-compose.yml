version: "3.8"

networks:
  backend:
  frontend:
volumes:
  db_data:
    driver: local
services:
  nginx:
    image: nginx:latest
    ports:
      - 8000:80 
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ./backend:/var/www/html 
    depends_on:
      - app
      - mysql
    networks:
      - backend

  frontend:
    container_name: frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile
    volumes:
      - ./frontend:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - 3000:3000
    environment:
      - NODE_ENV=development
    networks:
      - frontend

  app:
    build:
      context: ./backend
      target: dev
    volumes:
      # - ./app/xdebug.ini:/usr/local/etc/php/conf.d/xdebug.ini
      - ./backend:/var/www/html # To change project folder name
    ports:
      - '9000:9000'
    depends_on:
      - mysql
    networks:
      - backend

  mysql:
    image: mysql:8.0
    ports:
      - '3309:3306'
    environment:
      MYSQL_ROOT_PASSWORD: rootPass
      MYSQL_USER: user # To change database user
      MYSQL_PASSWORD: rootPass # To change database user password
      MYSQL_DATABASE: eventdb # To change database name
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - backend

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: dev_pma
    links:
      - mysql
    environment:
      PMA_HOST: mysql
      PMA_PORT: 3309
      PMA_ARBITRARY: 1
    restart: always
    ports:
      - 8183:80
    networks:
      - backend

